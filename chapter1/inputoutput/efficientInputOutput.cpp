#include<bits/stdc++.h>

using namespace std;
int main(){
    //this statement makes io faster
    ios::sync_with_stdio(0);
    //this statement makes io faster
    cin.tie(0);

    int a,b;
    //using the functions from c to make io efficient
    scanf("%d %d", &a, &b);
    printf("%d %d", a, b);

    return 0;
}
