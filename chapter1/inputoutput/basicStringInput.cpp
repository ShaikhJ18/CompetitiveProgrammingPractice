#include<bits/stdc++.h>

using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    //store the sentence
    string sentence;
    //get the whole sentence from the command line
    getline(cin,sentence);

    cout << sentence;
    //output the string to the command line

    return 0;
}